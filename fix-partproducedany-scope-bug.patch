--- a/packages/osrs-engine/src/config/player/PlayerModelLoader.ts	2026-08-29 16:21:08.295219477 +0000
+++ b/packages/osrs-engine/src/config/player/PlayerModelLoader.ts	2026-08-29 16:21:08.296598363 +0000
@@ -49,6 +49,7 @@
             const kitId = appearance.kits[part] ?? -1;
             if (kitId === -1) continue;
             expectedParts.push(part); // TEMP DIAGNOSTIC
+            let partProducedAny = false; // TEMP DIAGNOSTIC (moved outside try so it's in scope below)
             try {
                 const kit = this.idkTypeLoader.load(kitId) as any;
                 const ids: number[] = kit.modelIds ?? [];
@@ -65,7 +66,6 @@
                     );
                 }
                 // --- END TEMP DIAGNOSTIC ---
-                let partProducedAny = false; // TEMP DIAGNOSTIC
                 for (let i = 0; i < ids.length; i++) {
                     const md = this.modelLoader.getModel(ids[i]);
                     if (!md) {
