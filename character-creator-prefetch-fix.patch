--- a/packages/osrs-engine/src/config/player/PlayerModelLoader.ts	2026-08-29 16:43:58.972179334 +0000
+++ b/packages/osrs-engine/src/config/player/PlayerModelLoader.ts	2026-08-29 16:43:58.977463546 +0000
@@ -39,52 +39,17 @@
         const missesBefore = this.modelLoader.missCount ?? 0;
         const modelDatas: ModelData[] = [];
         const colors = Array.isArray(appearance.colors) ? appearance.colors : [];
-        // --- TEMP DIAGNOSTIC: remove after the null-flash is found ---
-        const expectedParts: number[] = [];
-        const producedParts: number[] = [];
-        // --- END TEMP DIAGNOSTIC (setup) ---
 
         // Body parts 0..6 per Idk: 0 head, 1 jaw, 2 torso, 3 arms, 4 hands, 5 legs, 6 feet
         for (let part = 0; part <= 6; part++) {
             const kitId = appearance.kits[part] ?? -1;
             if (kitId === -1) continue;
-            expectedParts.push(part); // TEMP DIAGNOSTIC
-            let partProducedAny = false; // TEMP DIAGNOSTIC (moved outside try so it's in scope below)
             try {
                 const kit = this.idkTypeLoader.load(kitId) as any;
                 const ids: number[] = kit.modelIds ?? [];
-                // --- TEMP DIAGNOSTIC: catch a resolved kit with no modelIds at all ---
-                if (ids.length === 0) {
-                    console.warn(
-                        "[cc-diag] kit had zero modelIds",
-                        "part:",
-                        part,
-                        "kitId:",
-                        kitId,
-                        "kit:",
-                        kit,
-                    );
-                }
-                // --- END TEMP DIAGNOSTIC ---
                 for (let i = 0; i < ids.length; i++) {
                     const md = this.modelLoader.getModel(ids[i]);
-                    if (!md) {
-                        // --- TEMP DIAGNOSTIC ---
-                        console.warn(
-                            "[cc-diag] getModel miss",
-                            "part:",
-                            part,
-                            "kitId:",
-                            kitId,
-                            "modelId:",
-                            ids[i],
-                            "missCount:",
-                            this.modelLoader.missCount,
-                        );
-                        // --- END TEMP DIAGNOSTIC ---
-                        continue;
-                    }
-                    partProducedAny = true; // TEMP DIAGNOSTIC
+                    if (!md) continue;
                     // Apply kit recolors/retextures first
                     if (kit.recolorFrom) {
                         for (let r = 0; r < kit.recolorFrom.length; r++) {
@@ -117,14 +82,7 @@
                     }
                     modelDatas.push(md);
                 }
-            } catch (e) {
-                // --- TEMP DIAGNOSTIC ---
-                console.warn("[cc-diag] part threw", "part:", part, "kitId:", kitId, "err:", e);
-                // --- END TEMP DIAGNOSTIC ---
-            }
-            // --- TEMP DIAGNOSTIC ---
-            if (partProducedAny) producedParts.push(part);
-            // --- END TEMP DIAGNOSTIC ---
+            } catch {}
         }
 
         // Merge extra wearable object models (e.g., boots, helms)
@@ -178,31 +136,9 @@
         if (modelDatas.length === 0) {
             return undefined;
         }
-        // --- TEMP DIAGNOSTIC: flag when we expected N body parts but built fewer ---
-        if (producedParts.length !== expectedParts.length) {
-            console.warn(
-                "[cc-diag] PARTIAL BODY BUILT",
-                "expected parts:",
-                expectedParts,
-                "produced parts:",
-                producedParts,
-                "kits:",
-                appearance.kits,
-            );
-        }
-        // --- END TEMP DIAGNOSTIC ---
         if ((this.modelLoader.missCount ?? 0) !== missesBefore) {
             // A body-part model is still streaming in; don't build (and let
             // callers cache) a partial player model.
-            // --- TEMP DIAGNOSTIC ---
-            console.warn(
-                "[cc-diag] miss-guard tripped, returning undefined this frame",
-                "missesBefore:",
-                missesBefore,
-                "missesAfter:",
-                this.modelLoader.missCount,
-            );
-            // --- END TEMP DIAGNOSTIC ---
             return undefined;
         }
 
@@ -328,6 +264,31 @@
         return this.buildStaticModel(workingAppearance, extras);
     }
 
+    /**
+     * Warms the sparse model cache for every IdentityKit body-part model
+     * (all genders, all styles). `models` is a deferred cache index — model
+     * payloads stream in on first use rather than being downloaded upfront —
+     * so cycling through unseen kit styles one at a time can each briefly
+     * miss while its model streams in. Calling this once, up front, fires
+     * off all those fetches together instead of discovering them reactively
+     * per click. Safe to call repeatedly; already-resident models are cheap
+     * no-ops and misses are swallowed (they've simply re-queued the fetch).
+     */
+    prefetchAllKitModels(): void {
+        const count = this.idkTypeLoader.getCount?.() ?? 0;
+        for (let kitId = 0; kitId < count; kitId++) {
+            try {
+                const kit = this.idkTypeLoader.load(kitId) as any;
+                const ids: number[] = kit?.modelIds ?? [];
+                for (let i = 0; i < ids.length; i++) {
+                    try {
+                        this.modelLoader.getModel(ids[i]);
+                    } catch {}
+                }
+            } catch {}
+        }
+    }
+
     private partCoveredByEquipment(
         part: number,
         equippedSlots: Set<EquipmentSlot>,
--- a/apps/client/src/engine/game/widgets/PlayerDesignController.ts	2026-08-29 16:43:58.974433389 +0000
+++ b/apps/client/src/engine/game/widgets/PlayerDesignController.ts	2026-08-29 16:43:58.978558613 +0000
@@ -3,6 +3,7 @@
 import type { ObjTypeLoader } from "@august/osrs-engine/config/objtype/ObjTypeLoader";
 import { PlayerAppearance } from "@august/osrs-engine/config/player/PlayerAppearance";
 import { PLAYER_BODY_RECOLOR_TO_1 } from "@august/osrs-engine/config/player/PlayerDesignColors";
+import { PlayerModelLoader } from "@august/osrs-engine/config/player/PlayerModelLoader";
 import type { SeqTypeLoader } from "@august/osrs-engine/config/seqtype/SeqTypeLoader";
 import type { VarManager } from "@august/osrs-engine/config/vartype/VarManager";
 import type { ModelLoader } from "@august/osrs-engine/model/ModelLoader";
@@ -35,6 +36,9 @@
 export class PlayerDesignController {
     // PlayerDesign (679) is client-side; keep a local editable appearance even before a world player exists.
     private playerDesignAppearance?: PlayerAppearance;
+    // Set once the very first time the design screen is used this session, so
+    // prefetchKitModelsOnce() only fires its warm-up pass a single time.
+    private hasPrefetchedKitModels = false;
 
     constructor(private readonly deps: PlayerDesignControllerDeps) {}
 
@@ -42,6 +46,31 @@
         this.playerDesignAppearance = undefined;
     }
 
+    /**
+     * Warms the model cache for every body-kit style up front, so browsing
+     * Design options doesn't repeatedly discover "model not loaded yet" one
+     * click at a time. `models` is a deferred/streamed cache index, so the
+     * first time any given style is selected it can otherwise miss for a
+     * frame while its model streams in. Cheap to call multiple times.
+     */
+    private prefetchKitModelsOnce(): void {
+        if (this.hasPrefetchedKitModels) return;
+        this.hasPrefetchedKitModels = true;
+        try {
+            const idkTypeLoader = this.deps.getIdkTypeLoader();
+            const objTypeLoader = this.deps.getObjTypeLoader();
+            const modelLoader = this.deps.getModelLoader();
+            const textureLoader = this.deps.getTextureLoader();
+            if (!idkTypeLoader || !objTypeLoader || !modelLoader || !textureLoader) return;
+            new PlayerModelLoader(
+                idkTypeLoader,
+                objTypeLoader,
+                modelLoader,
+                textureLoader,
+            ).prefetchAllKitModels();
+        } catch {}
+    }
+
     handleWidgetAction(childId: number): boolean {
         // PlayerDesign (Interface group 679) is a client-side appearance editor.
         // Cache widgets in this group are mostly empty containers; clicks should mutate the local
@@ -81,6 +110,7 @@
 
         const idkTypeLoader = this.deps.getIdkTypeLoader();
         if (!idkTypeLoader) return true;
+        this.prefetchKitModelsOnce();
         const pa = this.getOrInitPlayerDesignAppearance();
         if (!pa) return true;
 
--- a/apps/client/src/engine/rendering/render/init/shaders.ts	2026-08-29 16:43:58.975888380 +0000
+++ b/apps/client/src/engine/rendering/render/init/shaders.ts	2026-08-29 16:43:59.012608256 +0000
@@ -802,35 +802,6 @@
                                             ? localAppearance || wAny.playerAppearance
                                             : wAny.playerAppearance || localAppearance;
 
-                                    // --- TEMP DIAGNOSTIC: remove after the null-flash is found ---
-                                    if (
-                                        ((params.widget as any)?.uid >>> 16) === 679 &&
-                                        appearanceSrc
-                                    ) {
-                                        const kitsSnapshot = Array.isArray(appearanceSrc.kits)
-                                            ? appearanceSrc.kits.slice(0, 7).join(",")
-                                            : "MISSING";
-                                        const g = (globalThis as any);
-                                        if (g.__ccLastKits !== kitsSnapshot) {
-                                            console.log(
-                                                "[cc-diag]",
-                                                performance.now().toFixed(1),
-                                                "kits:",
-                                                kitsSnapshot,
-                                                "gender:",
-                                                appearanceSrc.gender,
-                                                "src:",
-                                                localAppearance
-                                                    ? "ecs"
-                                                    : wAny.playerAppearance
-                                                      ? "widget"
-                                                      : "none",
-                                            );
-                                            g.__ccLastKits = kitsSnapshot;
-                                        }
-                                    }
-                                    // --- END TEMP DIAGNOSTIC ---
-
                                     if (host.playerModelLoader2D && appearanceSrc) {
                                         const gender =
                                             typeof appearanceSrc.gender === "number"
