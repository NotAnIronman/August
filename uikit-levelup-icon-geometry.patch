diff --git a/client/common/packets/ServerPacketId.ts b/client/common/packets/ServerPacketId.ts
index fa25d24..3da2a37 100644
--- a/client/common/packets/ServerPacketId.ts
+++ b/client/common/packets/ServerPacketId.ts
@@ -207,7 +207,7 @@ export const SERVER_PACKET_LENGTHS: Record<ServerPacketId, number> = {
     [ServerPacketId.WIDGET_SET_ANIMATION]: 6, // uid(4) + animId(2)
     [ServerPacketId.WIDGET_SET_PLAYER_HEAD]: 4, // uid(4)
     [ServerPacketId.WIDGET_SET_QUEST_LIST]: -2,
-    [ServerPacketId.WIDGET_SET_SPRITE]: 12, // uid(4) + archiveId(4) + frame(4)
+    [ServerPacketId.WIDGET_SET_SPRITE]: 28, // uid(4) + archiveId(4) + frame(4) + x(4) + y(4) + width(4) + height(4), -1 = unchanged
 
     [ServerPacketId.CHAT_MESSAGE]: -1,
     [ServerPacketId.FRIENDS_CHAT_UPDATE]: -2,
diff --git a/client/game/OsrsClient.ts b/client/game/OsrsClient.ts
index ac1b77e..6a9f2fe 100644
--- a/client/game/OsrsClient.ts
+++ b/client/game/OsrsClient.ts
@@ -2443,6 +2443,10 @@ export class OsrsClient {
                 const uid = Number(payload.uid) | 0;
                 const archiveId = Number(payload.archiveId) | 0;
                 const frame = Math.max(0, Number(payload.frame) | 0);
+                const overrideX = Number(payload.x);
+                const overrideY = Number(payload.y);
+                const overrideWidth = Number(payload.width);
+                const overrideHeight = Number(payload.height);
                 const w = this.widgetManager?.getWidgetByUid(uid);
                 if (w) {
                     (w as any).type = 5;
@@ -2453,6 +2457,26 @@ export class OsrsClient {
                     (w as any).cacheSpriteToken = undefined;
                     (w as any).cacheSpriteArchiveId = archiveId >= 0 ? archiveId : undefined;
                     (w as any).cacheSpriteFrame = archiveId >= 0 ? frame : undefined;
+                    // -1 (the "omitted" sentinel from the server) means leave
+                    // that dimension as whatever the widget already had.
+                    // This is what fixes a sprite stretching to fill a
+                    // component whose native rect wasn't sized for it.
+                    if (overrideWidth >= 0) {
+                        (w as any).rawWidth = overrideWidth;
+                        (w as any).width = overrideWidth;
+                    }
+                    if (overrideHeight >= 0) {
+                        (w as any).rawHeight = overrideHeight;
+                        (w as any).height = overrideHeight;
+                    }
+                    if (overrideX >= 0) {
+                        (w as any).rawX = overrideX;
+                        (w as any).x = overrideX;
+                    }
+                    if (overrideY >= 0) {
+                        (w as any).rawY = overrideY;
+                        (w as any).y = overrideY;
+                    }
                     markWidgetInteractionDirty(w);
                     this.widgetManager.invalidateWidgetRender(w);
                 }
diff --git a/client/network/packet/ServerBinaryDecoder.ts b/client/network/packet/ServerBinaryDecoder.ts
index b81097f..f302315 100644
--- a/client/network/packet/ServerBinaryDecoder.ts
+++ b/client/network/packet/ServerBinaryDecoder.ts
@@ -884,6 +884,10 @@ export function decodeServerPacket(data: Uint8Array | ArrayBuffer): DecodedServe
                     uid: reader.readInt(),
                     archiveId: reader.readInt(),
                     frame: reader.readInt(),
+                    x: reader.readInt(),
+                    y: reader.readInt(),
+                    width: reader.readInt(),
+                    height: reader.readInt(),
                 },
             };
 
diff --git a/client/network/serverConnection/types/widgets.ts b/client/network/serverConnection/types/widgets.ts
index 2440222..4323dee 100644
--- a/client/network/serverConnection/types/widgets.ts
+++ b/client/network/serverConnection/types/widgets.ts
@@ -52,6 +52,14 @@ export type WidgetServerPayload =
           uid: number;
           archiveId: number;
           frame: number;
+          /** Optional geometry override (use when the target component's
+           *  native rect isn't sized/shaped for the replacement sprite -
+           *  e.g. it was a wide text-adjacent slot, not a square icon).
+           *  Omit any of these to leave that dimension unchanged. */
+          x?: number;
+          y?: number;
+          width?: number;
+          height?: number;
       }
     | {
           action: "set_npc_head";
diff --git a/server/gamemodes/vanilla/scripts/levelup.ts b/server/gamemodes/vanilla/scripts/levelup.ts
index ebb90b8..c477c86 100644
--- a/server/gamemodes/vanilla/scripts/levelup.ts
+++ b/server/gamemodes/vanilla/scripts/levelup.ts
@@ -138,9 +138,12 @@ function showSkillLevelUp(
     // The interface's baked-in cache icon for this component is either
     // missing, wrong, or absent for this revision - push the named sprite
     // (::Rename'd via the sprite gallery, e.g. "skill.attack") explicitly.
-    // Wrapped defensively: this is a nice-to-have on top of the popup, and
-    // must never be able to take the actual text down with it if something
-    // about the lookup or the send goes wrong.
+    // The component's native rect isn't a square icon slot (it stretched a
+    // replacement sprite across the whole width), so this also overrides
+    // the geometry to a small square in the top-left. Wrapped defensively:
+    // this is a nice-to-have on top of the popup, and must never be able to
+    // take the actual text down with it if something about the lookup or
+    // the send goes wrong.
     try {
         const iconRef = resolveSpriteRefByName(`skill.${skillName.toLowerCase()}`);
         if (iconRef) {
@@ -149,6 +152,10 @@ function showSkillLevelUp(
                 uid: (LEVELUP_INTERFACE_ID << 16) | (targetComp & 0xffff),
                 archiveId: iconRef.archiveId,
                 frame: iconRef.frame,
+                x: 4,
+                y: 4,
+                width: 32,
+                height: 32,
             });
         }
     } catch (err) {
diff --git a/server/src/network/messages.ts b/server/src/network/messages.ts
index 4009411..0870260 100644
--- a/server/src/network/messages.ts
+++ b/server/src/network/messages.ts
@@ -1123,6 +1123,10 @@ function encodeWidgetToBinary(payload: WidgetServerPayload): Uint8Array {
                 payload.uid,
                 payload.archiveId,
                 payload.frame,
+                payload.x ?? -1,
+                payload.y ?? -1,
+                payload.width ?? -1,
+                payload.height ?? -1,
             );
         case "set_transparency":
             return serverEncoder.encodeWidgetSetTransparency(payload.uid, payload.transparency);
diff --git a/server/src/network/packet/ServerBinaryEncoder.ts b/server/src/network/packet/ServerBinaryEncoder.ts
index b3db7d9..b6de7a5 100644
--- a/server/src/network/packet/ServerBinaryEncoder.ts
+++ b/server/src/network/packet/ServerBinaryEncoder.ts
@@ -609,11 +609,23 @@ export class ServerBinaryEncoder {
         return this.buffer.toPacket(ServerPacketId.WIDGET_SET_ITEM);
     }
 
-    encodeWidgetSetSprite(uid: number, archiveId: number, frame: number): Uint8Array {
+    encodeWidgetSetSprite(
+        uid: number,
+        archiveId: number,
+        frame: number,
+        x: number,
+        y: number,
+        width: number,
+        height: number,
+    ): Uint8Array {
         this.buffer.reset();
         this.buffer.writeInt(uid);
         this.buffer.writeInt(archiveId);
         this.buffer.writeInt(frame);
+        this.buffer.writeInt(x);
+        this.buffer.writeInt(y);
+        this.buffer.writeInt(width);
+        this.buffer.writeInt(height);
         return this.buffer.toPacket(ServerPacketId.WIDGET_SET_SPRITE);
     }
 
diff --git a/server/src/widgets/WidgetManager.ts b/server/src/widgets/WidgetManager.ts
index 0a0e6e4..05be57a 100644
--- a/server/src/widgets/WidgetManager.ts
+++ b/server/src/widgets/WidgetManager.ts
@@ -59,11 +59,18 @@ export type WidgetAction =
            *  use, for native (non-UIKit) interfaces that need one (e.g. the
            *  level-up popup). See client/common/uikit/spriteNames.ts for
            *  looking up a name -> archiveId/frame, and
-           *  SpriteNameCatalogFile.ts to resolve it server-side. */
+           *  SpriteNameCatalogFile.ts to resolve it server-side.
+           *  x/y/width/height optionally override the widget's native rect -
+           *  needed when that rect wasn't shaped for the replacement sprite
+           *  (e.g. a wide text-adjacent slot instead of a square icon). */
           action: "set_sprite";
           uid: number;
           archiveId: number;
           frame: number;
+          x?: number;
+          y?: number;
+          width?: number;
+          height?: number;
       }
     /** 0 is opaque and 255 is fully transparent. */
     | { action: "set_transparency"; uid: number; transparency: number }
