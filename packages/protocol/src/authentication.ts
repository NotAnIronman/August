/** Maximum length accepted for a player account name. */
export const MAX_ACCOUNT_NAME_LENGTH = 12;

/** Characters accepted in a player account name after trimming and lowercasing. */
export const ACCOUNT_NAME_PATTERN = /^[a-z0-9 _-]+$/;

/** Minimum length accepted for a player account password. */
export const MIN_PASSWORD_LENGTH = 8;

/** Maximum length accepted for a player account password. */
export const MAX_PASSWORD_LENGTH = 20;
