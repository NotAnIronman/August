/** OSRS chatbox channel IDs that trigger client-side actions. */
export const enum ChatMessageType {
    /** Clicking this line makes the client send OPPLAYER2 (Trade). */
    TRADE_REQUEST = 101,
}
