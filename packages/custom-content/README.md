# `@august/custom-content`

Shared custom-item and custom-content definitions used by both applications. Engine cache
construction accepts this behavior through an adapter. This package may depend on
`@august/game-model`, `@august/protocol`, and `@august/osrs-engine`, but never an application;
the engine never imports this package.
